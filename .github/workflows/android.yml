name: Android

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: set up JDK 11
      uses: actions/setup-java@v2
      with:
        java-version: '11'
        distribution: 'adopt'

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
    - name: Build with Gradle
      run: ./gradlew build

      # Start create debug apk artifact
    - name: Upload APK
      uses: actions/upload-artifact@v2
      with:
        name: appDebugBuild
        path: app/build/outputs/apk/debug/app-debug.apk

      # Start notify telegram bot
    - name: Notify Telegram
      uses: yanzay/notify-telegram@v0.1.0
      with:
        chat: -1001299618498 # Get chat id using terminal: curl https://api.telegram.org/bot{bot_token}/getUpdates
        token: 1715455171:AAEWxaYOPq29uaZb_FcrqJk6SzWr_QiA2Ew # Use Telegram @@BotFather
        status: ${{ job.status }} # do not modify this line